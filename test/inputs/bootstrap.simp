;--
;   this is a scratchpad used to bring up the parser.
;--

this.is.a.compound.symbol
const dict

; this is being run from the build directory
import "../test/parser/a.simp"() as poop

namespace blart {
    public
    plart
    namespace nugrbs {
        private
        protected
    }
}
