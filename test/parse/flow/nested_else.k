;-------------------------------------------------------------------------------
; nested in else
;-------------------------------------------------------------------------------

func name()() {

    if(comp.name == 0) {
        bulart = 0
    }
    else {
        goop()()
        if(comp.name == 0) {
            bulart = 0
        }
        else {
            goop()()
            if(comp.name == 0) {
                bulart = 0
            }
            else {
                goop()()
            }
        }
    }
}
