;-------------------------------------------------------------------------------
; nested in if
;-------------------------------------------------------------------------------

func name()() {

    if(comp.name == 0) {
        bulart = 0
        if(comp.name == 0) {
            bulart = 0
            if(comp.name == 0) {
                bulart = 0
            }
            else {
                goop()()
            }
        }
        else {
            goop()()
        }
    }
    else {
        goop()()
    }
}
