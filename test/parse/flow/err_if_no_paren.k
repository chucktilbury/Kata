;-------------------------------------------------------------------------------
; imbalanced parens after if
;-------------------------------------------------------------------------------

func name()() {

    if(comp.name == 0 {
        bulart = 0
    }
    else(1 != 2) {
        goop()()
    }
}