;-------------------------------------------------------------------------------
; yield requires a compound reference
;-------------------------------------------------------------------------------

func name()() {

	for(symbol in expression) {
		yield(123)
	}
}
