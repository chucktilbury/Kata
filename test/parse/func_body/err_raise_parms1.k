;-------------------------------------------------------------------------------
; raise expects a symbol and a formatted string
;-------------------------------------------------------------------------------

func name()() {
	RAISE(a_symbol, 123)
}
