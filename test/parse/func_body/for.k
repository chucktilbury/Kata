;-------------------------------------------------------------------------------
; basic for loop
;-------------------------------------------------------------------------------

public function virtual some_name()() {

	for(name in strings) {
		asdfe += 1
		for(signed name in ((name + 1) != 0)) {
			funcname()(name)
		}
	}
}
