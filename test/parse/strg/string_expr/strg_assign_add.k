;-------------------------------------------------------------------------------
; Use the assign add operator in relation to a string
;-------------------------------------------------------------------------------

function name()() {
    name += "this is a string"
}
