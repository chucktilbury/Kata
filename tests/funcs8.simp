
/*
 * Verify that a function can accept a while statement
 */
namespace name {
int func1() {

    int var = 1;

    while(var > 0) {
        var -= 1;
    }

    var = 1;
    do {
        var -= 1;
    } while(var > 0);
}
}
