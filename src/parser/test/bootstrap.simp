;--
    this is a comment
;--


; so is this...
; num bla = asd.wer[sdf[12]][44]
namespace ns1 {

    ; num sdflkj()  {df sd}
    class theclass {
        struct a_struct {
            const num a_number
            string a_string
            ; type is a keyword
            const user.trype another_one
        }
        ; declaration, not a definition
        private string meth(num val, string val, list val)
        private number meth1()
    }

}

namespace ns2 {
    public
    class withit (theclass) {
        protected
        struct lkasdkjl {}
        dict some_data
    }
}
